# Learner Agent Guide

`understanding_agent.py` turns learner free text into structured profile data through the shared LLM provider. Preserve its provider protocol and validation/error behavior; use `core.llm_client` for provider access.
