# Agent Guide

AI agents encapsulate learner-understanding/provider protocols. They should receive a provider abstraction, validate structured output, and expose deterministic errors. Do not embed credentials or direct database writes in agents.
