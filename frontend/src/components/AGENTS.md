# Component Guide

`AppShell` provides authenticated navigation/layout and `ui.tsx` provides shared primitives. Keep components accessible, reuse existing styling patterns, and use the installed `lucide-react` icons for controls.
